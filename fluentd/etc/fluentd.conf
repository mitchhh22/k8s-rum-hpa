<source>
  type forward
  format json
</source>
<filter **>
  @type parser
  format json
  key_name log
  reserve_data true
</filter>

<match *.*>
  type elasticsearch
  host elasticsearch
  logstash_format true
  flush_interval 10s
</match>
