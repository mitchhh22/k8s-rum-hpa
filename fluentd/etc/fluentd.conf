<source>
  type forward
</source>
<filter **>
  @type parser
  format json
  key_name log
  reserve_data true
</filter>

<match *.*>
  type elasticsearch
  host elasticsearch
  user fluentd
  password noice1
  ssl_verify false
  logstash_format true
  flush_interval 10s
</match>
